# EcoScan Environment Variables

# Optional: URL to your hosted Teachable Machine model
# If not provided, the app will run in simulation mode with realistic fake predictions
# The URL should point to the folder containing model.json and metadata.json
VITE_MODEL_URL=

# Example:
# VITE_MODEL_URL=https://your-username.github.io/ecoscan-model
